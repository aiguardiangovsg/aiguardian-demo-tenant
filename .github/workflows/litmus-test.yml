name: Litmus Test

on:
  push:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  run-litmus-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Litmus Test
        uses: dsaidgovsg/aiguardian-litmus-test@v0.0.13
        with:
          base_url: ${{ vars.LITMUS_BASE_URL }}
          run_name: ${{ github.run_id }}-${{ github.run_attempt }}
          endpoint: ${{ vars.ENDPOINT }}
          cookbook: ${{ vars.COOKBOOK }}
          num_of_prompts: ${{ vars.NUM_OF_PROMPTS || '0'}}
          api_key: ${{ secrets.LITMUS_KEY }}
          debug_mode: 'FALSE'
